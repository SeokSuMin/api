version: '3'

services:
    api:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: 'api'
        restart: unless-stopped
        ports:
            - '3002:3002'
        volumes:
            - /app/node_modules
            - ./:/app
        environment:
            - TZ=Asia/Seoul
